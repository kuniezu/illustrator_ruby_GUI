# Illustrator Ruby GUI - 要件定義書

**更新日**: 2026年4月5日
**対象ファイル**: `Illustrator Ruby GUI.jsx`
**ベース**: [illustrator-ruby](https://github.com/inaniwaudon/illustrator-ruby) by いなにわうどん（MIT License）
**制作**: 鈴木明世（北海道博物館）

---

## 1. 概要

Adobe Illustrator上で、選択したテキストフレームの各文字にルビ（ふりがな）をGUIで対話的に設定・配置するExtendScript。

### 主な特徴
- 横長レイアウトのGUIで、文字一覧とルビ入力欄を左側、設定パネルを右側に配置
- 単一テキストフレームを対象（複数選択時は先頭1つを処理）
- 横書き・縦書きの両方に対応
- グループ化により複数文字に一つのルビを振れる
- ルビのフォント・色は対象文字から自動取得

---

## 2. GUI構成

### ウィンドウレイアウト
```
Window (row orientation)
├─ 左パネル（文字表示エリア）
│  ├─ 再描画ボタン + ヒントテキスト
│  └─ テキスト内容パネル (column orientation)
│     ├─ ページコンテナ（1ページ分の文字のみ表示）
│     │  └─ スクロールグループ（ページ切替時に破棄・再作成）
│     │     └─ 行グループ × N（現ページ分のみ）
│     │        └─ 文字グループ × M（ルビ入力 + 文字ボタン）
│     └─ ページナビゲーション行
│        ├─ 「< 前」ボタン
│        ├─ ページ表示ラベル（"1 / 5"）
│        └─ 「次 >」ボタン
└─ 右サイドバー（設定パネル、固定幅260px）
   ├─ 選択中のルビ（表示・編集）
   ├─ グループ操作（グループ化/解除/クリア）
   ├─ 条件設定（サイズ▲▼、間隔▲▼、はみだし、小文字の大文字変換）
   ├─ 学習漢字ハイライト
   ├─ 実行/キャンセルボタン
   ├─ 使い方パネル
   └─ クレジット表示
```

### サイズ計算
- **右サイドバー**: 固定幅 260px
- **左パネル**: 画面幅の60%を上限、800〜1200pxの範囲
- **ウィンドウ全体幅**: 左パネル + 右サイドバー + 余白(40px)
- **ウィンドウ高さ**: 画面高さの80%を基準、700〜1000pxの範囲

### 文字表示エリア
- 各文字は40px幅の列（上: ルビ入力欄16px高、下: 文字ボタン24px高）
- 行間スペース: 2px
- 1行あたりの文字数: `Math.floor(contentWidth / 42)`
- ページ送り方式: 表示領域に収まる行数を1ページとし、ナビゲーションボタンでページ切替
  - ScriptUIの制限により、クリッピング領域外の子要素はレイアウト計算されないため、スクロールではなくページ送りを採用
  - 1ページあたりの行数: `Math.floor(contentAreaHeight / 42)`
  - ページ切替時はUIを破棄・再作成し、データ（rubyData, selectedCharIndices）は保持

### 右サイドバーの各パネル
| パネル | 内容 |
|---|---|
| 選択中のルビ | 選択文字表示、ルビ入力フィールド |
| グループ操作 | グループ化/グループ解除/選択一括解除ボタン（縦並び）、選択数表示 |
| 条件設定 | サイズ(%) + ▲▼ボタン（5%刻み）、間隔(%) + ▲▼ボタン（1%刻み）、はみだし（調整なし/変形）、小文字の大文字変換チェック |
| 学習漢字ハイライト | なし / 小1〜小6までで未習 |
| ボタン | 実行/キャンセル（横並び） |
| 使い方 | 基本操作・グループ・条件設定・学習漢字の説明 |
| クレジット | 「制作: 鈴木明世（北海道博物館）」（中央揃え） |

### 使い方パネル（サイドバー内）
操作ガイドを4セクションに分けて表示（フォントサイズ7pt、spacing 0）:

| セクション | 内容 |
|---|---|
| ● 基本 | Tab/Shift+Tabで漢字間移動 / 各欄クリックで直接編集も可 |
| ● グループ | 漢字を複数クリックで選択 → グループ化で複数文字にルビ |
| ● 条件設定 | フォント・色：対象文字に準拠 / サイズ：親文字に対する比率 / 間隔：ルビと親文字の距離 / はみだし：文字上に収める幅調整 |
| ● 学習漢字ハイライト | ◆付き＝未習漢字（ルビ推奨） |

---

## 3. 機能仕様

### 3.1 文字選択とルビ入力
- 文字ボタンクリックで選択（トグル）
- 選択状態は `selectedCharIndices`（characterIndexベース）で管理し、ページをまたいで保持
- ルビ入力欄に直接入力、またはサイドバーの編集パネルから入力
- Tabキーで次の漢字（またはルビ入力済み）のルビ入力欄へ移動（ひらがな・カタカナ等はスキップ）
- Shift+Tabで逆方向に移動
- Tab/Shift+Tabはページ境界を越えて自動遷移（次/前ページに移動し、該当する入力欄にフォーカス）
- ScriptUIのデフォルトTab動作は常に抑止し、文字ボタンにフォーカスが移動しないようにする

### 3.2 グループ化
- 複数文字を選択し「グループ化」で一つのルビを設定
- グループ内の先頭文字にルビを入力、他の文字は「-」表示で無効化
- 「グループ解除」で個別ルビに戻す
- 「選択一括解除」で文字ボタンの選択状態を一括解除（ルビデータは保持）

### 3.3 ルビ配置処理

#### パフォーマンス最適化
- テキストフレームの `duplicate()` + `createOutline()` はフレームごとに**1回だけ**実行し、全ルビで `compoundPaths` を共有
- 可視文字インデックスはルックアップテーブルとして事前計算し、ルビごとの再計算を回避
- `textFrame.textRange.characters` は1回だけ取得してキャッシュ（IPC通信の削減）
- ルビ配置ループは `try-finally` で囲み、エラー時も一時アウトラインを確実に削除

#### フォント・色
- ルビのフォント: 対象文字（親文字）と同じ `textFont` を使用
- ルビの色: 対象文字の `fillColor` を使用

#### サイズ・間隔
- ルビサイズ = 親文字サイズ × サイズ比率（デフォルト40%、▲▼で5%刻み調整）
- ルビと親文字の間隔 = 親文字サイズ × 間隔比率（デフォルト3%、▲▼で1%刻み調整）

#### トラッキング
- 基本トラッキング: 100
- ルビが親文字より長い場合: トラッキングを減少（最小 -100）

#### はみだし処理
| モード | 動作 |
|---|---|
| 調整なし | トラッキング調整のみ。はみ出しはそのまま |
| 変形 | ルビの水平/垂直スケールを縮小して親文字幅に収める |

#### 位置計算
- 親文字のアウトライン（`createOutline()`）から実際の位置・サイズを取得
- `compoundPathItems` は逆順（最後の文字 = index 0）
- 事前計算済みの `visibleIndexMap` で `characterIndex`（contents上の位置）をアウトラインのパスインデックスに変換
  - `createOutline()` でcompoundPathが生成されない文字（CR, LF, Tab, 半角スペース, 全角スペース, emスペース）をスキップしてカウント
- 横書き: ルビを親文字の上に配置、水平中心を合わせる
- 縦書き: ルビを親文字の右側に配置、垂直中心を合わせる
- 中央揃え（`Justification.CENTER`）を縦横共通で設定
- 字形補正: アウトラインの高さ/幅が文字サイズの50%未満の場合（「一」「二」「三」等）、隣接文字の上端位置を参照して補正。隣に正常な文字がない場合はアウトライン中心からem squareを推定（`baseSize * 0.8`）

### 3.4 小文字の大文字変換（捨て仮名変換）
- チェック時: 小書き仮名（ぁ→あ、ッ→ツ等）を通常仮名に変換

### 3.5 再描画機能
- ウィンドウサイズ変更後に「再描画」ボタンを押すと、新しいウィンドウサイズに基づいてUIを再構築
- 再描画時は以下を引き継ぐ:
  - `rubyData`（入力済みルビデータ）
  - `winBounds`（ウィンドウ位置・サイズ）
  - `currentPage`（表示中のページ番号）
  - `kanjiHighlightGrade`（学習漢字ハイライト設定）
- `maxCharsPerLine` が新しい幅で再計算されるため、1行あたりの文字数が変わる
- ページ送り方式により、コンテンツは常に表示領域に収まる

### 3.6 テキストフレーム
- 単一テキストフレームのみ処理
- 複数選択時はアラートを表示し、先頭1つを処理

---

## 4. コード構成

```
Illustrator Ruby GUI.jsx
├─ 注意書き（Array.prototypeポリフィルを使用しない理由）
├─ 捨て仮名変換テーブル・関数
│  ├─ suteganaList[]
│  └─ convertSutegana()
├─ 文字分類
│  ├─ kanjiCodes[]
│  └─ isKanji()（forループで実装、ポリフィル不使用）
├─ 学習漢字データ
│  ├─ gakushuKanji（小1〜小6、計1,026字）
│  └─ getKanjiGrade()（O(1)検索、キャッシュ付き）
├─ テキストフレーム取得
│  └─ getSelectedTextFrames()
├─ メインGUI
│  └─ showRubyGUI(textFrames, options)
│     ├─ データ構造（charInfoMap, selectedCharIndices, pageState）
│     ├─ ウィンドウ・レイアウト構築（再描画ボタン、ページナビ含む）
│     ├─ 事前計算（全文字→視覚行の分割、UI非依存）
│     ├─ renderPage()（1ページ分のUI作成・イベントハンドラ設定）
│     ├─ イベントハンドラ（選択、グループ化、Tab移動、ページ遷移等）
│     └─ 実行ボタン → placeRubys()
├─ ルビ配置
│  ├─ placeRubys(textFrames, rubyData, settings)
│  │  └─ フレームごとに1回だけアウトライン化、visibleIndexMap事前計算
│  └─ placeOneRuby(textFrame, group, characterIndex, baseLength, rubyText, settings, isVertical, compoundPaths, visibleIndexMap, characters)
├─ ユーティリティ
│  └─ getVisibleCharacterIndex(textFrame, characterIndex)（後方互換用、placeRubysでは未使用）
└─ エントリーポイント
   └─ main()（再描画ループ: rubyData/winBounds/currentPage/kanjiHighlightGradeを保持してUI再構築）
```

---

## 5. 動作環境

- Adobe Illustrator（ExtendScript対応バージョン）
- macOS / Windows
- 横書き・縦書きテキストフレーム対応

---

## 6. 既知の制限事項

- ScriptUIの制約により、ルビ入力欄のフォントサイズ変更が反映されない場合がある
- ScriptUIのレイアウトエンジンは柔軟性に欠けるため、サイズ指定はシンプルに保つ
- ScriptUIは `maximumSize` によるクリッピング領域外の子要素をレイアウト計算しない（Adobe側の根本的制限）。このため従来のスクロール方式を廃止し、ページ送り方式を採用
- ウィンドウリサイズに自動追従しないため、再描画ボタンによる手動リフレッシュが必要
- グループ化は同一ページ内の連続する文字に対してのみ操作可能（ページをまたぐグループ化は不可）

---

## 7. 技術的注意事項

### Array.prototypeポリフィルの不使用
- ExtendScriptでは `Object.defineProperty` で `enumerable: false` にできないため、`Array.prototype` にメソッドを追加するとIllustratorの内部コードが `for...in` で配列を走査した際に干渉し、クラッシュの原因となる
- 本スクリプトではポリフィルを使用せず、必要な処理はすべて `for` ループで記述する

### パフォーマンスとクラッシュ防止
- `createOutline()` はフレームごとに1回だけ実行（ルビごとの繰り返し実行はメモリ枯渇によるクラッシュの原因となる）
- 一時アウトラインは `try-finally` で確実に `remove()` する（リソースリーク防止）
- `textFrame.textRange.characters` や `textFrame.contents` はループ前に1回だけ取得してキャッシュ

### createOutline() とスペース文字
- `createOutline()` は以下の文字に対して `compoundPathItem` を**生成しない**:
  - CR (U+000D)、LF (U+000A)、Tab (U+0009)
  - 半角スペース (U+0020)、全角スペース (U+3000)、emスペース (U+2003)
- `visibleIndexMap` の事前計算ではこれらの文字をスキップしてカウントする
- スキップ対象に漏れがあると、対象文字以降のルビ位置がズレる（スペース1個につき1文字分）

### contents と characters のインデックス
- `textFrame.contents` と `textFrame.textRange.characters` は同じ長さ（改行 `\r` を含む）
- `characterIndex` は `contents` ベースで算出され、`characters[]` のアクセスにもそのまま使用可能

---

## 8. 学習漢字ハイライト機能

### 概要
博物館の展示パネル等で「小学N年生までに習う漢字にはルビを振らない」運用を支援するため、未習漢字を視覚的にハイライト表示する機能。

### データ
- 2020年 学習指導要領（別表）に準拠した小1〜小6の学習漢字（計1,026字）をスクリプト内に埋め込み
- `getKanjiGrade(ch)` 関数で漢字の学年を O(1) で検索（初回呼び出し時にキャッシュ構築）

### GUI
- 条件設定パネル内に「学習漢字ハイライト」ドロップダウンを配置
- 選択肢: なし / 小1までで未習 / 小2までで未習 / ... / 小6までで未習
- デフォルト: 「小4までで未習」

### 挙動
- 全ての入力フィールドは常に有効（入力を制限しない）
- 未習漢字（選択学年より上の漢字 + 学習漢字外の漢字）のボタンに `◆` マーカーを付与
- graphics APIが対応している場合はボタンテキスト色も変更
- ハイライトはルビ入力のガイドであり、熟語の判断はユーザーに委ねる
- redraw時にハイライト設定を保持
