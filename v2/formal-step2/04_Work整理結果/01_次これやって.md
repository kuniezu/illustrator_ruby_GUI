# 次これやって

- dispatch_id: `gate-d-final-runtime-20260913-0011`
- state: `USER_RUNTIME_REQUIRED`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- reviewed_audit_commit: `1b5e3ef7701a13d2de6a170c2a726ebceb5620e0`
- reviewed_code_commit: `14a6872b87180501e0bff140584c590f8e267a16`
- source_of_truth: `このファイルのみ`

## 今回やること

workerは実装しない。ユーザーがIllustrator実機でGate D最終runtime checkpointを1回だけ行う。

目的は「横書き長文で複数Annotationを実用的に扱える最小ワークフロー」の実DOM証跡をまとめて確認すること。

1. fresh expendable documentで、横書きAreaTextに複数行へ折り返す程度の日本語長文を置く。本文中に少なくとも3つの別々の漢字語を含める。
2. 通常の `v2/formal-step2/Formal Multi Step2.jsx` を実行する。
3. 少なくとも3つのoccurrenceへreadingを設定し、有効化してSaveする。
4. managed rubyが複数生成され、本文・unmanaged objectが保持され、明らかな重複managed outputがないことを確認する。
5. そのうち1件をsuppress/disableしてSaveし、そのrubyだけが除外されることを確認する。
6. 同じ1件をre-enableしてSaveし、rubyが復帰し、他のmanaged rubyが重複増殖しないことを確認する。
7. readingを1件だけ変更して再Saveし、一世代置換されることを確認する。
8. palette/scriptをいったん閉じて再実行し、同じsourceで既存状態が復元され、再Saveしてもmanaged rubyが重複しないことを確認する。
9. 長文の実折返し後でも各rubyが対応する親文字付近に残り、source/unmanaged objectが保持されていることを確認する。

## PASS条件

- 複数readingを1回のworkflowで扱える。
- 複数managed rubyが保存される。
- suppress / re-enableが対象1件だけに効く。
- re-edit / re-saveで旧世代が置換され、重複増殖しない。
- palette/script再起動後も既存状態を再利用し、重複しない。
- source本文とunmanaged objectが保持される。
- 実折返し後も対応関係が実用上破綻しない。

## 失敗時

その時点でSTOPし、追加操作を繰り返さない。スクリーンショットとcopyable debug/statusをChatGPTへ返す。

## STOP条件

- Luna/Codex/Work workerはコード変更しない。
- 旧A-E/F matrixや過去の単一Annotation runtimeは繰り返さない。
- この1回のruntime結果をChatGPTがレビューするまで次工程へ進まない。
- PR / merge / Issue closeはしない。
