# 次これやって

- dispatch_id: `gate-d-planner-boundary-propagation-20260914-0436`
- state: `READY_FOR_WORKER_START`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- base_code_head: `0be0647529db8f7d8afa925dd9a50617299e53ac`
- source_of_truth: `このファイルのみ`
- shared_checklist: `v2/formal-step2/04_Work整理結果/04_実装・実機前チェックリスト.md`

## supervisor再演で見つかったblocker

前dispatchのno-op editor save修正自体は正しい。

ただし、実機シナリオ `再編集` の行またぎ → planner `split-hint-required` → boundary evidence保存 → 局所分割picker、をproduction pathで再演したところ、planner boundaryがproduction persistence/UI層まで実際には届かない可能性を確認した。

`FormalSegments.plan()` は multi-line 時に `decision.boundaries` / `decision.unresolvedBoundaries` を返す。

しかし `FormalMultiOrchestration.planOne()` は現在、返却objectへ

- `decision`
- `reasons`

は載せる一方、`decision.boundaries` / `decision.unresolvedBoundaries` をtop-levelへ転記していない。

`FormalMultiRenderer.plan()` もそのresultをそのままplansへ入れるだけ。

一方、production persistence pathは `plan.plans[i].boundaries` / `plan.plans[i].unresolvedBoundaries` を読んでoccurrence render evidenceへ保存し、failureDetailにも同じtop-level fieldを使う。

そのため `split-hint-required` 自体は返っても、boundary arraysが空になり、paletteの局所分割pickerへ `再｜編集` のsuggested boundaryが届かない可能性がある。

これは今回の実機checkpointを直接止めるため、実機前blockerとする。

## 今回やること

### 1. planner boundaryをlosslessにproduction resultへ伝播

- `FormalSegments.plan()` の `decision.boundaries` / `decision.unresolvedBoundaries` / 必要なら `reusableHints` を、orchestration → multi-renderer → persistence/UIまでlosslessに運ぶ。
- 最小変更を優先する。top-levelへ明示転記する場合は、decisionとの二重source-of-truthにならないよう契約をテストする。
- `split-hint-required` では対象occurrenceにlocal boundaryがpersistされること。
- `split-hint-boundary-mismatch` でも unresolved boundary semanticsを壊さない。
- stale caseで存在しないboundaryを捏造しない。

### 2. production操作順を直接回帰化

最低限、次をpure/integration/staticで証明する。

- `FormalSegments.plan()` が `split-hint-required` + boundary `[1]` を返すfixture。
- `FormalMultiOrchestration.planOne()` / `FormalMultiRenderer.plan()` 後にも同じboundaryが取得できる。
- persistence bridgeがそのboundaryをfailure bundleのoccurrenceへ保存する。
- callback persisted noteをpalette authorityにした後もrenderBoundariesが残る。
- no-op `saveEditor()` 後もboundaryが残る。
- split button相当で `FormalSplitBoundaryUi.choose(occurrence.surface, occurrence.renderBoundaries)` にsuggested boundaryが渡る。
- close/reopen後も同じboundaryが残る。

今回の実機シナリオ全体を、可能な範囲で1本の回帰としてつなげる。

### 3. completion report SHA誤記を再発させない

前dispatchの02は implementation commit を
`0be0647a8db7f70ec1b42c93855a83218794b4a6`
と記録しているが、このSHAは存在しない。

実際のimplementation commitは
`0be0647529db8f7d8afa925dd9a50617299e53ac`
（`Preserve render evidence on no-op editor saves`）。

共有checklistには既に「remote存在確認」があるため、今回の02では実際にGitHub remote上に存在するSHAだけを記録する。

## Guardrails

- readingの比例配分・自動推測は禁止。
- FormalSegmentsの保守的split契約を緩めない。
- strict native fit / tracking `0,-25,-50,-75,-100` を変更しない。
- native AreaText geometry / FIT-06 / FIT-07を変更しない。
- copy-on-write / ownership / persistence identity / source-unmanaged / restart identityを変更しない。
- broad UI redesignをしない。
- user runtime手順をこの01へ書かない。
- PR / merge / Issue closeはしない。

## 検証

- focused segments / orchestration / multi-renderer / persistence / workflow / UI tests
- split-hint-required boundary end-to-end propagation regression
- split-hint-boundary-mismatch regression
- STATE-01〜06 regressions
- known-failure minimum regression pack
- full Node suite
- ExtendScript compatibility lint
- gate-0
- `git diff --check`

workerはIllustrator実機を実行しない。

## 完了報告

`02_今これやったよ.md`をこのdispatch idで上書きし、placeholder/誤SHAなしでfinal値を記録する。
actual implementation commitはremote存在確認後に記録する。
`03_作業ログ.md`へ原因と修正を追記する。

## STOP条件

- 実装・検証・checklist/catalog/02/03更新・commit/pushまで終えたらSTOP。
- 次の実機手順をworker自身で決めない。
- 次taskへ進まない。
