# 次これやって

- dispatch_id: `gate-d-vm-harness-unblock-20260914-2334`
- state: `READY_FOR_WORKER_START`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- base_code_head: `968b3ac77ca7693d804c209cc347824c2393f849`
- source_of_truth: `このファイルのみ`
- shared_checklist: `v2/formal-step2/04_Work整理結果/04_実装・実機前チェックリスト.md`

## context

前dispatch `gate-d-planner-render-evidence-consistency-20260914-2121` は実装途中でBLOCKEDになった。

worker報告:

- Node VM隔離環境とExtendScript実行環境の差異により、複数occurrenceのgenerated Bridge production-equivalent regressionが失敗。
- HEADは `968b3ac77ca7693d804c209cc347824c2393f849` のまま。
- working treeに4ファイルの未commit変更あり。
- `git diff --check` は警告なし。
- 未検証なのでcommit/pushしていない。

このSTOP判断は正しい。今回のtaskは**既存4ファイル変更を保持したまま、テストハーネス差異を解消し、元dispatchを検証可能な状態へ戻すこと**。

## 最重要 guardrail

- 現在の未commit 4ファイル変更を捨てない。`reset --hard` / checkoutで破棄しない。
- remote同期時もlocal変更を保護する。必要ならstashではなく、まずdiffを退避確認してから安全な方法を選ぶ。
- productionコードを「Node VMで通すためだけ」に変えない。
- production-equivalent regressionをstatic `assert.match`へ格下げしない。
- generated BridgeTalk bodyとtest helperを別ロジックに分岐させない。
- 実際のproduction contractに欠陥があることを確認した場合のみproductionを修正する。
- Illustrator実機は実行しない。

## unblock A: Node VM vs ExtendScript差異を特定する

失敗をまず最小再現し、次のどの差異かを具体的に記録する。

- `$.evalFile` / `$` の有無・module loading semantics
- `File` / `Folder` のconstructor/read/open behavior
- `app` / `activeDocument` / `getPageItemFromUuid`
- `TextType` / `TextOrientation`
- global symbolの注入順序
- vm contextの`Array` / `Object` realm差異（`instanceof Array`を含む）
- ExtendScript global scopeとNode CommonJS/module scopeの差異
- source note / native manifest facade read-write double
- generated body内のES3 syntax/runtime差異

失敗message/stackを02または03へ残す。単に「VM差異」とだけ書かない。

## unblock B: production-equivalent harnessの方針

最優先は、**production generated bodyをそのまま実行する**test harnessを成立させること。

許容するmock/stubはIllustrator host boundaryだけに限定する。

- `$.evalFile` はruntimePathsからproduction sourceを読み、同一VM global contextへ評価するtest doubleにする。
- `File` はproduction bodyが必要とする最小contractを実装し、runtime source path / stage log用途を区別する。
- `app.activeDocument` / source frame / note / contents / uuid lookupは最小DOM doubleで再現する。
- native physical output creationまで不要なscenarioなら、その境界はproduction module API契約を満たすhost doubleで置き換える。
- `FormalMultiStore` / renderer / persistence mappingをtest独自実装で再現しない。production sourceをevalして使う。

もしNode `vm`のcross-realm `instanceof Array`等がproductionと異なることが原因なら、test fixture/dataを同一realmで生成するか、harness側でproduction realmへ正しく渡す。**その差だけを理由にproductionのES3-safe contractを変更しない。**

## unblock C: 元dispatchの必須検証を復活させる

ハーネスが動いたら、前dispatchをそのまま完遂する。

1. aggregate noncomplete時:
   - unresolved item → durable unresolved
   - failed item → durable failed
   - hidden-confirmed → durable hidden
   - planner-complete sibling → durable pending（terminal completeへ昇格禁止）
2. aggregate failed / unresolvedがwire callbackとpersisted bundle top-level statusで一致。
3. terminal native success後のみeligible visible occurrenceがdurable complete。
4. 複数 `split-hint-required` occurrenceのboundary evidenceが**1 saveで全件durable**。
5. close/reopen roundtripでも同じ。
6. debug console `B-render-persist: send=true` は1 sendにつき1表示。

### 必須 production-equivalent regression

少なくとも1本はgenerated `renderedBridgeBody()`そのものをVMで実行し、複数occurrenceのnoncomplete planをsource noteへ書き戻した結果を`FormalMultiStore.read(frame.note)`で検証する。

static source assertionのみでは不可。

## broad audit: 今回のblockerの上下左右

ハーネス復旧後、今回の「planner complete ≠ terminal render complete」を次で照合する。

- upstream: `FormalSegments` / `planOne` / `FormalMultiRenderer.plan`
- durable boundary: failure bundle / `FormalMultiStore`
- downstream: callback parse / palette bundle authority / `statusText`
- terminal: native success / pre-activation failure / post-activation cleanup failure
- horizontal: hidden / suppressed / logical-only / unsupported / re-enable / source re-resolution

特にUIでdurable `pending`がinput-readyの`ready`表示へ誤fallbackしないか確認する。もし現行`FormalMultiWorkflow.renderStatus()` + `statusText()`で`pending`が見えず`ready`になるなら、これは同型のsemantic propagation bugとして同batchで最小修正＋回帰する。

ただし、文字列`complete`そのものを一律blocker扱いしない。item planner status / aggregate planner status / terminal render evidenceのlayerを区別する。

## tests / validation

- failing production-equivalent generated bridge testをPASSへする
- focused persistence / multi-renderer / workflow / UI
- multiple-boundary durable persistence executable regression
- callback persisted-status consistency regression
- pending-vs-ready UI regression（問題が確認された場合）
- existing hidden / recovery / send=false / blocked UI regressions
- minimum regression pack
- full Node suite
- ExtendScript compatibility lint
- gate-0
- `git diff --check`

## completion report

`02_今これやったよ.md` をこのdispatch idで上書きする。

必ず記載:

- BLOCKED原因の具体的なNode VM/ExtendScript差異
- harness修正内容
- production変更が必要だったか／不要だったか
- production-equivalent testの正確なtest名
- multiple-boundary durable evidence結果
- planner/render evidence mapping結果
- UI pending/ready横断監査結果
- 40桁full implementation SHA
- remote存在確認
- focused / minimum / full / ES3 / gate-0 / diff-check

## STOP条件

- 元dispatchの実装＋harness＋全検証＋02/03/checklist/catalog必要更新＋commit/pushまで完了したらSTOP。
- 再びVM差異で詰まる場合は、未検証production変更をcommitせずBLOCKEDでSTOPし、失敗test名・stack・原因候補を具体的に報告する。
- PR / merge / Issue closeはしない。
- 次の実機手順をworker自身で決めない。
