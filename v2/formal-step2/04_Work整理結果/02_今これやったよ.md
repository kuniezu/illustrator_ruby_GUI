# 今これやったよ

- dispatch_id: `gate-d-completion-audit-20260912-2349`
- state: `AUDIT_COMPLETED_NOT_CLOSE_READY`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- synced_head: `5f1db0b971422afd66a6746acd04eaa85b78b25b`
- source_of_truth: `01_次これやって.md` only
- result_commit: `この監査記録を含むcommit（最終SHAは完了報告で確定）`

## 調査した主要ファイル / 契約

- `Formal Multi Step2.jsx`: 横書きAreaText選択、occurrence一覧、reading/enabled編集、局所分割・隣接結合、unresolved review navigation、保存UI。
- `multi.js` / `multi-store.js` / `multi-renderer.js` / `workflow.js`: occurrenceとAnnotationの分離、source-relative anchor、永続化、抑制・再有効化、review queue、render specification。
- `persistence-adapter.jsx` / `area-text-native-integration.js` / `area-text-native-transaction-coordinator.js`: previous manifest read-before-write、prepare→verify→activate→retire→finish、cleanup/recovery、persisted-note readback。
- `native-renderer.js` / `area-text-native-backend.jsx` / `area-text-native-output-identity.js`: native AreaText creation、strict-fit、tracking fallback `0,-25,-50,-75,-100`、glyph-ink placement、physical identity、source/unmanaged ownership。
- `known-failure-regression-catalog.md` / `migration-parity-gate.md` / `minimum-regression-pack.cjs` と既存tests/log: 31 failure IDs、9 migration-parity dimensions、実行済みassertion anchors。

## 現状評価

- productionのFormal Multi Step2 save pathは、occurrenceをAnnotationへ投影し、native RenderSpecを生成し、BridgeTalk側でnative AreaTextのprepare/verify/activate/retire/finishへ接続されている。
- unresolved review navigation、explicit suppress、re-enable、suppressed項目のqueue除外、再有効化後のqueue復帰は既存のpure workflowを利用して実装済み。
- catalog 31 IDs、minimum pack、full Node 359/359、ExtendScript compatibility 38 files、gate-0 10/10、diff-check PASSという既存の自動検証記録がある。今回の監査では再実行していない。
- FIT-07 runtimeはPASS。`ruby-glyph-cleanup-failed`は解消し、native rubyは描画された。軽微な縦位置ずれはユーザー判断により最終調整へ延期し、現時点のblockerにはしない。

## Gate D必須blocker一覧

1. 最終目的「横書き長文で複数Annotationを実用的に扱える最小ワークフロー」のIllustrator実機証跡が未完了。FIT-07 runtimeは単一の`今回 → こんかい`保存と目視確認であり、複数occurrenceへのreading設定、複数managed outputの保存、抑制・再有効化、一覧/review状態の実機整合までは確認していない。
2. 同じ複数Annotation workflowについて、save→re-edit→saveの一世代置換、source/unmanaged object preservation、palette再起動後の重複なしを実機で確認していない。pure/integration testsは実DOMの代替ではない。
3. actual AreaText reflowは既知のIllustrator依存であり、width setterによる自動証明はできない。長文fixtureの実際の折返しと、折返し後の複数Annotation render/ownershipをclose-readyのruntime evidenceとして一度確認する必要がある。

## deferred polish

- FIT-06/FIT-07後に残るnative rubyの軽微な縦位置調整。ユーザー判断によりGate D blockerにしない。
- proportional font、隣接Annotation、長いreadingの見た目の追加チューニング。現行のstrict-fit / tracking契約を緩めず、実用結果に応じて後続判断する。
- `createOutline()`方式の最終性能判断。今回の目的外。

## non-goal

- tokenizer本実装、Reading Provider / dictionary
- vertical text
- legacy migration implementation / `formal-step2-output:v1` co-ownership
- packaging最終化
- broad renderer/JLREQ redesign
- PR作成、merge、Issue close

## 判定 / 次に必要な最小slice

- 判定: `Gate D close-ready候補` ではあるが、現時点ではclose-readyではない。
- 次に必要な最小slice: production code変更ではなく、ユーザー実機で横書き長文・複数Annotationの一括runtime checkpointを1回実施し、複数reading、save→re-edit→save、suppression/re-enable、source/unmanaged preservation、重複なしを確認する。失敗時はその1回で停止し、copyable debug/statusを記録する。

## tests / runtime

- 今回の監査で新規testは実行していない。
- 今回の監査でIllustrator runtimeは実行していない。
- 既存の最終validation記録はminimum pack PASS、full Node 359/359 PASS、ExtendScript compatibility PASS、gate-0 10/10 PASS、git diff --check PASS。

開始: 2026-09-13 00:05:29 JST
終了: commit後の最終報告で確定
