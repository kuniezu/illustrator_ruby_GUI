# 今これやったよ

- dispatch_id: `gate-d-splitguard-multifixture-proof-20260915-1105`
- state: `COMPLETED`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- base_code_head: `f2780d25a3d3b96ac9c5c0a93dcd2f8e47af58c8`
- implementation_commit: `165b05efabde0a527704149db51d73b9ebe0b397`
- source_of_truth: `01_次これやって.md` only

## blocker A/B 修正

- A: `mergeAdjacentWithPlan()` はplan結果の件数や配列先頭に依存せず、merged targetのoccurrence IDからprojected annotation IDを解決して、全annotation結果から一意に照合するよう修正した。
- A: 複数occurrence bundleでも、選択したchild readingが全て確認済みならmerged targetのreading確認状態を保持し、siblingsを含むproduction-like fixtureでcurrent complete proofを成立させた。
- B: UIのguard付きmergeは`editorCandidate()`で一時bundleを作り、current observation/plannerとguard-aware mergeを一時bundle上で検証する。許可後だけlive bundle/revisionへ反映し、拒否時はdirty editorを含めてlive stateを変更しない。
- existing `FormalStep2Adapter.observe()`、`FormalMultiOrchestration.projectAndPlanAll()`、native/persistence/ownership lifecycleは再利用し、新しいIllustrator DOM操作やscope拡張は行っていない。

## 回帰・監査

- `multi occurrence guarded merge selects the middle target and remains atomic on reject`
- `current complete observation permits a guarded merge while current split evidence rejects it`
- `rejects merge that would restore a known split blocker before editor save`
- 10+ occurrence、targetがsiblingの中央、suppressed sibling、dirty editor candidate、target-only plan result照合を確認した。
- 新known-failure: `STATE-26`
- checklist A/Bへmulti target identity、confirmed merged reading、temporary candidate atomicityを追記した。

## validation

- focused workflow/UI/occurrences/re-resolution: 56/56 PASS
- minimum regression pack: 71 IDs PASS
- full Node suite: 410/410 PASS
- ExtendScript compatibility: 39 production files PASS、diagnostic entrypoint PASS
- gate-0: 10/10 PASS
- `git diff --check`: PASS
- base ancestor check: PASS
- Illustrator runtime: `NOT_RUN_BY_WORKER`
- strict-fit、tracking `0,-25,-50,-75,-100`、native AreaText contract、source/unmanaged preservation、verified persistence/ownership lifecycleは変更していない。

## git / runtime

- implementation commit `165b05efabde0a527704149db51d73b9ebe0b397` は対象branchへ通常push済み。
- PR作成、merge、Issue close: 実施していない。
- preflight開始: `2026-09-15 11:15:43 JST`
- 実装push確認: remote push成功を確認済み。
- checklist A/B: PASS
- runtimeはChatGPT remote review後まで実行しない。
