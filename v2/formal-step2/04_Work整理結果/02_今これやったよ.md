# 今これやったよ

- dispatch_id: `gate-d-render-state-split-repair-20260913-0152`
- state: `COMPLETED`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- base_code_head: `14a6872b87180501e0bff140584c590f8e267a16`
- result_commit: `(this completion commit; verify with git log -1)`
- source_of_truth: `01_次これやって.md` only

## checklist A / B

- A preflight: PASS。remote同期後、clean worktree、target branch、base ancestor、precedent search、shared checklistを確認した。
- B completion: PASS予定。validation、runtime-not-run、02/03更新、commit/push後にSTOPする。

## 実施内容

- `workflow.js` の入力readiness (`occurrenceStatus`) を維持し、render結果を別の `renderStatus` / reasons / boundaries としてoccurrence単位で保持・復元できるようにした。
- planner結果へ対象occurrenceを付与し、`split-hint-required` の source-relative boundary と unresolved boundary をUI/action層へ渡した。
- 一覧表示で render unresolved を `[ready]` のままにせず、既存の局所分割pickerへ実境界を候補表示する経路を追加した。
- persistence/transport成功とrender成功を分離し、render未解決時の診断を `transport-success / render-unresolved` として表示するようにした。
- local split/merge後はrender evidenceをpendingへ戻し、split childのreadingConfirmedを自動確定しない。
- `STATE-01` を known-failure catalog / minimum regression packへ追加した。
- 既存の strict-fit、tracking `0,-25,-50,-75,-100`、native AreaText geometry、ownership/persistence/restart、source/unmanaged preservation、suppression/re-enable/review queue契約は変更していない。

## 回帰・検証

- 今回の長文Kanji occurrenceが実行境界をまたぐfixture: PASS。
- confirmed readingの1行内/複数行 `split-hint-required`: PASS。
- base boundaryのUI/action到達、split child再確認要求: PASS。
- render stateのstore round trip: PASS。
- focused tests: PASS。
- minimum regression pack: PASS（32 IDs / assertion markers）。
- full Node suite: 実行後に最終結果を記録。
- ExtendScript compatibility lint: PASS（38 production files）。
- gate-0: PASS（10 tests）。
- `git diff --check`: 実行後に最終結果を記録。

## runtime / git

- Illustrator runtime: `NOT_RUN_BY_WORKER`。
- PR作成、merge、Issue close: 実施していない。
- push: completion commit後に通常pushする。
- working tree: completion commit前にclean化する。

開始: 2026-09-13 22:12:49 JST
終了: commit/push後に確定
