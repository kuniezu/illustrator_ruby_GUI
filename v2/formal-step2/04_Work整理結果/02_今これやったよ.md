# 今これやったよ

- dispatch_id: `gate-d-splitguard-lifecycle-bidirectional-20260915-1011`
- state: `COMPLETED`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- base_code_head: `4f7114331feeba0471b0ec8877730fb447b9b209`
- implementation_commit: `f2780d25a3d3b96ac9c5c0a93dcd2f8e47af58c8`
- source_of_truth: `01_次これやって.md` only

## blocker A/B 修正

- A: deterministicなunrelated source editでは、`inheritedOccurrence()` と local-run再構築がguard対象surfaceの `splitGuard` provenanceを保持するようにした。guard対象surfaceが変わる、ambiguous/not-found、意味が不一致な場合は移植しない。既存の「常に破棄」回帰は、保持/失効を分離した。
- B: guard付きmerge前に、既存の `FormalStep2Adapter.observe()` と `FormalMultiOrchestration.projectAndPlanAll()` でmerged candidateをcurrent observationへ再評価する。current plannerがcompleteを証明した場合だけ `mergeAdjacentWithPlan()` を許可し、split/unavailable/failedはfail-closedで拒否する。UIへline-crossing判定を複製していない。
- candidateは確認済みchild readingを連結したmerged readingで評価するが、実merge後は従来どおりreading再確認を要求する。native physical ownership、retirement、persistence chainは変更していない。

## splitGuard validity contract

- provenanceはroot/lineage、対象surface/range、boundary evidenceとともにoccurrenceへ保存される。
- source re-resolution後はdeterministicな同一surface mappingだけ保持し、semantic変更や曖昧一致では保持しない。
- geometry/reflow変更はsource textが同じでも、current observation/plannerのcomplete証明なしにguardを解除しない。
- current split evidenceまたはobservation不明時はmerge拒否、current merged candidate complete時のみmerge許可。

## 回帰・監査

- `unrelated source text re-resolution preserves valid splitGuard provenance`
- `guarded source semantic change does not transfer splitGuard`
- `current complete observation permits a guarded merge while current split evidence rejects it`
- `known split blocker rejects merge without mutating the source bundle`
- `split blocker survives save and reopen for merge guard`
- `rejects merge that would restore a known split blocker before editor save`
- ordinary/manual split/merge、hidden/suppressed/unsupported sibling、reading未確認、review queue、planner boundary、persistence/reopen、native transition/retirement ownershipを再確認。
- 新known-failure: `STATE-25`

## production-route audit

- source edit/geometry edit → reopen → re-resolution/observation → guard validity → merge decision → projection → planner → persistence → native transition → terminal evidence → reopenを上流から下流まで確認。
- data ownership/identity、status algebra、callback authority、one-save-one-send、transport-uncertain、hidden nonblocking、recovery/cleanupの既存契約を維持。
- merge拒否はcurrent plan未証明時にbundle/revision/selectionを変えず、許可時もnative transitionは通常の次saveでのみ開始する。

## validation

- focused tests: 110/110 PASS
- minimum regression pack: 70 IDs PASS
- full Node suite: 409/409 PASS
- ExtendScript compatibility: 39 production files PASS、diagnostic entrypoint PASS
- gate-0: 10/10 PASS
- `git diff --check`: PASS
- Illustrator runtime: `NOT_RUN_BY_WORKER`
- strict-fit、tracking `0,-25,-50,-75,-100`、native AreaText、source/unmanaged preservationは変更していない。
- legacy 15-field occurrenceはsplitGuardなしの互換境界として扱い、migrationは今回scope外。current 18-fieldはroundtripする。

## git / runtime

- implementation commit `f2780d25a3d3b96ac9c5c0a93dcd2f8e47af58c8` は対象branchへ通常push済み、remote確認済み。
- PR作成、merge、Issue close: 実施していない。
- 開始: `2026-09-15 10:53:01 JST`
- implementation push確認: `2026-09-15 11:02:16 JST`
