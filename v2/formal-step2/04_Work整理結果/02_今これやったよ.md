# 今これやったよ

- dispatch_id: `gate-d-split-merge-cycle-guard-20260915-0142`
- state: `COMPLETED`
- target_branch: `v2/area-text-native-one-shot-diagnostic`
- base_code_head: `2b3b423b0973d1196b630f7a2358dd6031fdd122`
- implementation_commit: `b81e9919fb6e36779485dd891cfb9f777574f5f2`
- source_of_truth: `01_次これやって.md` only

## 根本原因と修正

- `FormalLongText.mergeAdjacent()` はsplit childの `occurrenceId` とlineageを結合していたが、既知の `split-hint-required` provenanceを保持していなかった。そのためsplit→merge後にboundary evidenceが消え、同じline-crossing blockerを次回saveで復活させ得た。
- split childへ `splitGuard`（reason、base/unresolved boundaries）をdurableに保持し、multi storeのreopenでも復元するようにした。
- `splitGuard.reason === "split-hint-required"` のmergeだけを `merge-would-restore-split-blocker` でfail-closedにした。UIはeditor saveより前にguardを評価するため、拒否時にbundle/revision/selectionを変更しない。通常のsplit/mergeは従来どおり利用できる。
- merged identityをchild IDのまま再利用する経路を既知 blockerでは止め、lineageはsource historyとして保持する。split→merge→splitでは新しいchild IDが生成され、重複しない。

## 回帰・監査

- `known split blocker rejects merge without mutating the source bundle`
- `split blocker survives save and reopen for merge guard`
- `rejects merge that would restore a known split blocker before editor save`
- `splitAt edits one logical range and mergeAdjacent restores it`（通常merge維持）
- `hidden sibling is non-blocking and visible sibling still reaches renderer`（既存）
- split child reading再確認、review queue、suppression/re-enable、persistence/reopen、planner boundary、native ownership/lifecycleの既存回帰を再確認。
- 新known-failure: `STATE-24`

## 検証

- focused tests: 62/62 PASS
- minimum regression pack: 69 IDs PASS
- full Node suite: 406/406 PASS
- ExtendScript compatibility: 39 production files PASS、diagnostic entrypoint PASS
- gate-0: 10/10 PASS
- `git diff --check`: PASS
- Illustrator runtime: `NOT_RUN_BY_WORKER`
- strict-fit、tracking `0,-25,-50,-75,-100`、native AreaText、ownership/persistence/restart identity、source/unmanaged preservationは変更していない。

## git / runtime

- PR作成、merge、Issue close: 実施していない。
- remote implementation verification: `b81e9919fb6e36779485dd891cfb9f777574f5f2` が対象branchへ到達済み。
- 開始: `2026-09-15 01:46:18 JST`
- 検証完了: `2026-09-15 01:52:01 JST`
