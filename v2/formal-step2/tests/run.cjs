const test=require('node:test'),assert=require('node:assert/strict'),S=require('../segments.js');
test('one line remains one segment',()=>{let r=S.plan('一張羅','いっちょうら',[{start:0,end:3}],[],0,0);assert.equal(r.status,'complete');assert.equal(r.segments.length,1);});
test('wrapped line without hint is unresolved',()=>{assert.equal(S.plan('一張羅','いっちょうら',[{start:0,end:2},{start:2,end:3}],[],0,0).status,'unresolved');});
test('valid hint produces two segments',()=>{let r=S.plan('一張羅','いっちょうら',[{start:0,end:2},{start:2,end:3}],[{baseBoundaryAfter:2,readingBoundaryAfter:5,baseRevision:0,readingRevision:0}],0,0);assert.deepEqual(r.segments.map(x=>x.reading),['いっちょう','ら']);});
test('stale hint and proportional split are rejected',()=>{let h=[{baseBoundaryAfter:2,readingBoundaryAfter:5,baseRevision:0,readingRevision:0}];assert.equal(S.plan('一張羅','いっちょうら',[{start:0,end:2},{start:2,end:3}],h,1,1).status,'unresolved');assert.throws(()=>S.validateHints([{baseBoundaryAfter:2,readingBoundaryAfter:2,baseRevision:0,readingRevision:0},{baseBoundaryAfter:2,readingBoundaryAfter:5,baseRevision:0,readingRevision:0}],3,6));});
test('three boundaries must be monotonic and complete',()=>{let h=[{baseBoundaryAfter:1,readingBoundaryAfter:2,baseRevision:0,readingRevision:0},{baseBoundaryAfter:2,readingBoundaryAfter:4,baseRevision:0,readingRevision:0},{baseBoundaryAfter:3,readingBoundaryAfter:6,baseRevision:0,readingRevision:0}];let r=S.plan('一二三四','いちにさんよん',[{start:0,end:1},{start:1,end:2},{start:2,end:3},{start:3,end:4}],h,0,0);assert.equal(r.segments.length,4);});
